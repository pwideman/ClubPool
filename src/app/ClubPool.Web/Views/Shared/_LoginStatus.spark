<if condition="Model.IsLoggedIn">
  ${Model.Username} !{Html.ActionLink<UserController>(x => x.Logout(), "logout")}
</if>
<else>
  !{Html.ActionLink<UserController>(x => x.Login(string.Empty), "login")}
</else>